# Local models

Model weights are intentionally excluded from Git because they are large binary artifacts.
Use the existing download/import scripts or configure `EMBEDDING_MODEL` to a local model path.
